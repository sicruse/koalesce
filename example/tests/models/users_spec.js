var chai = require('chai');
var Users = require('../../models/users');

require('../setup');
chai.should();

describe('Users', function() {

	describe('Constructor', function () {

		it('can build a user instance', function (done) {

			

		});
	});

});